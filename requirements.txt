# Core dependencies
numpy>=1.20.0
opencv-python>=4.5.0

# GUI support (tkinter image rendering)
Pillow>=9.0.0

# AprilTag detection
# Pre-compiled binaries available for x86_64 and ARM (Raspberry Pi).
pupil-apriltags>=1.0.4

# QR-code detection (preferred backend)
# Requires libzbar0 system library:
#   Ubuntu/Debian:  sudo apt-get install libzbar0
#   macOS:          brew install zbar
pyzbar>=0.1.9

# YAML configuration file support
pyyaml>=5.0

# Optional: interactive 3-D SLAM visualisation (matplotlib backend)
# matplotlib>=3.5.0
